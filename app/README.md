# Tsuki

Android-first Flutter manga reader using AniList metadata, MangaDex English
chapters, Google sign-in, and Firestore sync. A local profile remains available
as a safe fallback when Firebase cannot initialize.

## Run

    flutter pub get
    flutter run

## Build

    flutter build apk --release

See `../docs/SETUP.md` for Firebase, signing, and demo-mode configuration.
